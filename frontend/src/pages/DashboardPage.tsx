import React, { useEffect, useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { dashboardApi, ApiError } from '../services/api';
import { fmtDateTime, fmtNumber, fmtPercent, stageLabels } from '../services/format';
import type { DashboardResponse, LeadResponse } from '../types';
import { BarList, DonutChart, LineChart } from '../components/charts';
import { Alert, Badge, Button, Card, KpiCard, LoadingPanel, PageHeader } from '../components/ui';
import { LeadForm } from '../components/forms/LeadForm';
export function DashboardPage(){const nav=useNavigate();const [data,setData]=useState<DashboardResponse|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [days,setDays]=useState(30);const [newLead,setNewLead]=useState(false);const range=useMemo(()=>({from:new Date(Date.now()-days*86400000).toISOString(),to:new Date().toISOString()}),[days]);const load=()=>{setLoading(true);setError('');dashboardApi.get(range).then(setData).catch(e=>setError((e as ApiError).message)).finally(()=>setLoading(false))};useEffect(load,[days]);const evolution=data?.commercialEvolution.map(x=>({label:x.label,value:x.points}))??[];const stages=data?.stageDistribution.map(s=>({label:stageLabels[s.stage],value:s.value,tone:s.stage}))??[];return <><PageHeader title="Visão Geral" description="Acompanhe atividade, desempenho e evolução comercial no período selecionado." actions={<><select className="lf-select lf-period-select" value={days} onChange={e=>setDays(Number(e.target.value))}><option value={7}>Últimos 7 dias</option><option value={30}>Últimos 30 dias</option><option value={90}>Últimos 90 dias</option></select><Button onClick={()=>setNewLead(true)}>+ Novo Lead</Button></>}/>{error&&<Alert tone="error">{error} <button className="lf-link" onClick={load}>Tentar novamente</button></Alert>}{loading?<div className="lf-grid-2"><LoadingPanel/><LoadingPanel/></div>:data&&<><div className="lf-kpi-grid"><KpiCard label="Leads ativos" {...data.activeLeads}/><KpiCard label="Novos Leads" {...data.newLeads}/><KpiCard label="Interações" {...data.interactions}/><KpiCard label="Pontos gerados" {...data.generatedPoints}/></div><div className="lf-grid-8-4"><Card><div className="lf-card-head"><div><h2 className="lf-card-title">Evolução comercial</h2><p className="lf-card-subtitle">Pontos gerados ao longo do período</p></div></div><LineChart data={evolution} series={[{key:'value',label:'Pontos'}]}/></Card><Card><div className="lf-card-head"><div><h2 className="lf-card-title">Leads por etapa</h2><p className="lf-card-subtitle">Distribuição atual</p></div></div><DonutChart data={stages}/></Card></div><div className="lf-grid-2"><Card><div className="lf-card-head"><div><h2 className="lf-card-title">Ranking resumido</h2><p className="lf-card-subtitle">Top 5 filiais por pontuação</p></div><Button variant="tertiary" size="sm" onClick={()=>nav('/ranking-filiais',{state:{from:'/dashboard'}})}>Ver ranking completo</Button></div><div className="lf-ranking-list">{data.branchRanking.length===0?<div className="lf-empty-text">Sem dados no período.</div>:data.branchRanking.slice(0,5).map((r,i)=><button className="lf-ranking-row" key={r.branchId} onClick={()=>nav(`/filiais/${r.branchId}`)}><span className="lf-ranking-num">{i+1}</span><span className="lf-ranking-name">{r.branchName}</span><span><strong>{fmtNumber(r.points)}</strong><small>{fmtPercent(r.conversionRate)} conversão</small></span><span className={r.trend>=0?'lf-trend-up':'lf-trend-down'}>{r.trend>=0?'↑':'↓'} {Math.abs(r.trend).toFixed(1)}%</span></button>)}</div></Card><Card><div className="lf-card-head"><div><h2 className="lf-card-title">Leads recentes</h2><p className="lf-card-subtitle">Cadastros mais recentes no seu escopo</p></div><Button variant="tertiary" size="sm" onClick={()=>nav('/leads')}>Ver todos os Leads</Button></div>{data.recentLeads.length===0?<div className="lf-empty-text">Nenhum Lead no período.</div>:<div className="lf-compact-list">{data.recentLeads.slice(0,8).map(l=><button key={l.id} onClick={()=>nav(`/leads/${l.id}`)}><div><strong>{l.name}</strong><span>{l.branchName} • {l.responsibleUserName}</span></div><div><Badge tone={l.stage==='CUSTOMER'?'success':l.stage==='LOST'?'danger':l.stage==='NEGOTIATION'?'warning':'primary'}>{stageLabels[l.stage]}</Badge><small>{fmtDateTime(l.lastInteractionAt)}</small></div></button>)}</div>}</Card></div></>}<LeadForm open={newLead} onClose={()=>setNewLead(false)} onSaved={(v:LeadResponse)=>{setNewLead(false);load();nav(`/leads/${v.id}`)}}/></>}
