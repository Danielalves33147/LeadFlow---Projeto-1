import React, { useState } from 'react';
import { Link, Navigate, useNavigate } from 'react-router-dom';
import { useAuth } from '../app/AuthContext';
import { ApiError } from '../services/api';
import { formatCnpj } from '../services/format';
import { Alert, Button, Field, Input } from '../components/ui';
function strength(v:string){let s=0;if(v.length>=8)s++;if(/[A-Z]/.test(v))s++;if(/[a-z]/.test(v))s++;if(/\d/.test(v))s++;if(/[^A-Za-z0-9]/.test(v))s++;return s<3?'Fraca':s<5?'Boa':'Forte'}
export function RegisterPage(){const {user,loading,register}=useAuth();const nav=useNavigate();const [f,setF]=useState({name:'',email:'',companyName:'',cnpj:'',password:'',confirm:'',acceptedTerms:false,acceptedPrivacy:false});const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [fields,setFields]=useState<Record<string,string>>({});if(!loading&&user)return <Navigate to="/dashboard" replace/>;const set=(k:string,v:string|boolean)=>setF(x=>({...x,[k]:v}));const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');setFields({});if(f.password!==f.confirm){setFields({confirm:'As senhas não coincidem.'});return}setBusy(true);try{await register({name:f.name,email:f.email,companyName:f.companyName,cnpj:f.cnpj,password:f.password,acceptedTerms:f.acceptedTerms,acceptedPrivacy:f.acceptedPrivacy});nav('/dashboard',{replace:true})}catch(e){const a=e as ApiError;setError(a.message);setFields(a.fieldErrors)}finally{setBusy(false)}};return <div className="lf-auth"><aside className="lf-auth-brand-panel"><div className="lf-auth-brand"><span className="lf-brand-mark">LF</span><strong>LeadFlow</strong></div><div><h1>Comece com uma base comercial organizada.</h1><p>Crie a conta principal da empresa e configure equipes, filiais e processos no mesmo ambiente.</p></div><div className="lf-auth-note">Segurança por perfil • Histórico rastreável • Interface responsiva</div></aside><main className="lf-auth-form-panel"><form className="lf-auth-form register" onSubmit={submit}><div className="lf-auth-mobile-brand">LeadFlow</div><div><h1>Criar conta</h1><p>Informe os dados essenciais da empresa.</p></div>{error&&<Alert tone="error">{error}</Alert>}<div className="lf-form-grid"><Field label="Nome completo" error={fields.name}><Input required minLength={2} maxLength={120} value={f.name} onChange={e=>set('name',e.target.value)}/></Field><Field label="E-mail corporativo" error={fields.email}><Input required type="email" value={f.email} onChange={e=>set('email',e.target.value)}/></Field><Field label="Nome da empresa" error={fields.companyName}><Input required value={f.companyName} onChange={e=>set('companyName',e.target.value)}/></Field><Field label="CNPJ" error={fields.cnpj}><Input required placeholder="00.000.000/0000-00" value={f.cnpj} maxLength={18} onChange={e=>set('cnpj',formatCnpj(e.target.value))}/></Field><Field label="Senha" error={fields.password} helper={`Força: ${strength(f.password)}. Use 8+ caracteres com letras, número e símbolo.`}><Input required minLength={8} type="password" value={f.password} onChange={e=>set('password',e.target.value)}/></Field><Field label="Confirmação da senha" error={fields.confirm}><Input required minLength={8} type="password" value={f.confirm} onChange={e=>set('confirm',e.target.value)}/></Field></div><div className="lf-legal-checks"><label className="lf-check"><input required type="checkbox" checked={f.acceptedTerms} onChange={e=>set('acceptedTerms',e.target.checked)}/><span>Li e aceito os <button type="button" className="lf-link">Termos de Uso</button>.</span></label><label className="lf-check"><input required type="checkbox" checked={f.acceptedPrivacy} onChange={e=>set('acceptedPrivacy',e.target.checked)}/><span>Li e aceito a <button type="button" className="lf-link">Política de Privacidade</button>.</span></label></div><Button size="lg" type="submit" disabled={busy}>{busy?'Criando conta...':'Criar conta'}</Button><p className="lf-auth-switch">Já possui uma conta? <Link to="/login">Entrar</Link></p></form></main></div>}
