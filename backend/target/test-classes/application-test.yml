spring:
  datasource:
    url: jdbc:h2:mem:leadflow;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
leadflow:
  demo:
    seed: false
  jwt:
    secret: test-secret-that-is-long-enough-for-hmac-signing-123456789
    expiration-seconds: 900
    refresh-expiration-seconds: 604800
